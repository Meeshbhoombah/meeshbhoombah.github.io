<!-- index.html-->
<!DOCTYPE html>
<div class='container'>
{{#each article}}
    {{with article}}
    <div class='row'>
        <div class='col-md-12'>
            <h2>{{title}}</h2>
        </div>
    </div>
    {{/with}}
{{/each}}
</div>

